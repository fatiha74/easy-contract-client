import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'easy-contract',
  webDir: 'dist/easy-contract',
  bundledWebRuntime: false
};

export default config;
